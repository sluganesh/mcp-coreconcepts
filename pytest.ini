[pytest]
testpaths = tests
addopts = -ra
filterwarnings =
    # Deliberate and documented: bulk_import_employees still uses MCP logging,
    # which the 2026-07-28 spec deprecates (see server.py).
    ignore:The logging capability is deprecated:mcp.shared.exceptions.MCPDeprecationWarning
